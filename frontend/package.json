{
  "name": "@camp-for-nepal/frontend",
  "private": true,
  "version": "0.1.0",
  "description": "Camp For Nepal frontend demo",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate:data": "node scripts/validate-data.mjs",
    "check:routes": "node scripts/check-routes.mjs",
    "smoke": "node scripts/smoke-render.mjs",
    "check": "npm run build && npm run validate:data && npm run check:routes && npm run smoke"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.18.2"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@vitejs/plugin-react": "^6.0.5",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.0"
  }
}
