// Site-wide defaults that do not belong to a particular public page.
import { SITE_NAME } from '../config/siteIdentity.js'

const siteSettings = {
  siteName: SITE_NAME,
  defaultLanguage: 'en',
  defaultCurrency: 'USD',
  demoMode: true,
}

export default siteSettings
