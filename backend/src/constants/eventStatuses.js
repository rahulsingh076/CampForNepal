export const EVENT_STATUSES = Object.freeze([
  'draft',
  'published',
  'cancelled',
  'completed',
  'archived',
])

export const PUBLIC_EVENT_STATUSES = Object.freeze(['published', 'cancelled', 'completed'])
export const DEFAULT_EVENT_STATUS = 'draft'

