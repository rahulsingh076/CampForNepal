export const MEDIA_TYPES = Object.freeze(['image', 'video', 'reel'])

export const MEDIA_SOURCE_TYPES = Object.freeze([
  'local_asset',
  'external_url',
  'youtube',
  'vimeo',
  'instagram',
  'facebook',
])

export const MEDIA_STATUSES = Object.freeze(['draft', 'published', 'hidden', 'archived'])

export const PUBLIC_MEDIA_STATUS = 'published'
export const DEFAULT_MEDIA_STATUS = 'draft'

export const PUBLIC_MEDIA_SOURCE_TYPES = Object.freeze([
  'local_asset',
  'external_url',
  'youtube',
  'vimeo',
  'instagram',
  'facebook',
])

